import { enrichEventWithDetails } from '@ui5/webcomponents-react-base/dist/Utils';
import { useCallback } from 'react';

var useLegendItemClick = function useLegendItemClick(handler, dataKeyExtractor) {
  return useCallback(function (payload, index, event) {
    if (typeof handler === 'function') {
      var dataKeyExtractorInternal = typeof dataKeyExtractor === 'function' ? dataKeyExtractor : function (e) {
        return e === null || e === void 0 ? void 0 : e.dataKey;
      };
      handler(enrichEventWithDetails(event, {
        dataKey: dataKeyExtractorInternal(payload),
        value: payload.value,
        chartType: payload.type,
        color: payload.color,
        payload: payload.payload,
        index: index
      }));
    }
  }, [handler, dataKeyExtractor]);
};

export { useLegendItemClick };
//# sourceMappingURL=useLegendItemClick.js.map
